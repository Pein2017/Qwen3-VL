- [x] Introduce dataset wrapper classes (target vs source) plus a registry that maps `fusion_config` entries (`dataset: bbu`, `dataset: coco`, etc.) to wrapper implementations with `domain`, `template_id`, `supports_augmentation`, and optional `poly_fallback`.
- [x] Refactor the offline fusion builder and the online `MultiSourceFusionDataset` to consume wrapper outputs, enforce fixed per-source ratios each epoch, and drop the `metadata.dataset` dependency in favor of wrapper-level configuration.
- [x] Rework augmentation/curriculum wiring so that only wrappers with `supports_augmentation=True` attach the augmentation preprocessor (target domain by default; sources skipped unless explicitly opted in).
- [x] Ensure `/data/public_data` converters (e.g., LVIS/COCO) emit canonical `poly`/`poly_points` geometries compatible with the new wrappers, including per-object fallback rules for overly complex polygons.
- [x] Update docs (`docs/DATA_AND_DATASETS.md`, `docs/DATA_AUGMENTATION.md`, `docs/PUBLIC_DATA.md`, `/data/public_data/README.md`, `docs/POLYGON_SUPPORT.md`) to explain the domain hierarchy, wrapper registry, and static-ratio fusion workflow (training uses target-domain evaluation only).
