extends: ./last_6.yaml

training:
  output_dir: ./output/smoke/tiny_pack
  run_name: tiny_fusion_smoke
  num_train_epochs: 2
  eval_steps: 10
  save_steps: 20
  save_total_limit: 1
  save_strategy: steps
  metric_for_best_model: eval_sft_loss
  greater_is_better: false

custom:
  fusion_config: configs/fusion/bbu_with_lvis_tiny.yaml
  train_sample_limit: null
  val_sample_limit: null
  dump_conversation_text: false
  # Disable augmentation curriculum for the tiny smoke to avoid degenerate boundaries
  augmentation_curriculum: null
