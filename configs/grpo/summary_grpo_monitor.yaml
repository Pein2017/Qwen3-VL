extends: summary_grpo_base.yaml

training:
  run_name: temperature_0.7-num_generations_4-vllm_colocate-monitor
  eval_steps: 50

rlhf:
  # Writes `completions.jsonl` (prompts + completions + rewards) into output_dir for debugging.
  log_completions: true
  num_completions_to_print: 2

