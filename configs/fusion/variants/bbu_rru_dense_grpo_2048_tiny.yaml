extends: ../base/dense_grpo.yaml

# Tiny fusion variant for smoke tests:
# - Uses the existing *_tiny.jsonl splits when available.

targets:
  - name: bbu_dense
    train_jsonl:
    val_jsonl:
  - name: rru_dense
    train_jsonl:
    val_jsonl:
