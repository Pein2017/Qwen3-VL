extends: ../base/summary_grpo.yaml

targets:
  - name: bbu_summary
    train_jsonl:
    val_jsonl:
    augmentation_enabled: true
    curriculum_enabled: false
  - name: rru_summary
    train_jsonl:
    val_jsonl:
    augmentation_enabled: true
    curriculum_enabled: false
  # Keep irrelevant summary data small to avoid dominating the policy gradient.
  - name: irrelevant_summary
    ratio: 0.1

sources: []
