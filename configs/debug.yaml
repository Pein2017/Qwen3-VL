extends:
  - base.yaml
  - components/model/qwen3_vl_4b.yaml
  - components/tuner/debug_lora.yaml
  - components/training/debug.yaml
  - components/data/loader_debug.yaml
  - components/custom/debug_data.yaml

# Minimal debug config used by tests and quick smoke runs.
# Targets fast config validation and single-GPU sanity checks.
