# codex-subagent-orchestrator References

These files are the **portable, canonical** docs for the `codex-subagent-orchestrator` skill.

Design goals:
- **Self-contained**: this directory should not require a specific repository layout to be useful.
- **Portable**: avoid hard-coding repo-specific absolute paths (e.g. `/data/...`).
- **Low drift**: repo-local `docs/**` may keep *thin stubs* pointing here, but should not duplicate full content.

Index:
- `CODEX_SUBAGENTS_ORCHESTRATION.md` — canonical runbook (protocol + contracts)
- `CODEX_SUBAGENTS_ORCHESTRATION_QUICKREF.md` — short checklist + tool list
- `CODEX_SUBAGENTS_TOY_DRILLS.md` — copy/paste toy stability drills (safe scratch-only)

